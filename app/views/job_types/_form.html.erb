<%= simple_form_for(@job_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :type_of_jobs %>
    <%= f.association :job_post %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
