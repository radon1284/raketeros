<%= form_for @job_type, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :type_of_jobs, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :type_of_jobs, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  job_types_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
